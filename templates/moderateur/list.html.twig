<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <style>
        body {
            background-color: #f0f0f0;
            font-family: Arial, sans-serif;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

        table {
            width: 100%;
            margin-top: 20px;
        }

        th, td {
            padding: 15px;
            text-align: left;
        }

        thead {
            background-color: #f8f9fa;
        }

        tr:nth-child(even) {
            background-color: #ffffff;
        }

        a {
            text-decoration: none;
            color: #007bff;
        }

        a:hover {
            text-decoration: underline;
        }

        .d-flex.align-items-center .ms-3 {
            text-align: left;
        }
    </style>
</head>
<body>
    <h1>liste Contenus</h1>

    {% if app.user %}
        <div class="mb-3">
           <a href="{{ path('app_logout') }}" style="display: block; text-align: right; text-decoration: none; margin-right:10px;">
                <button style="padding: 10px 20px; border: none; border-radius: 5px; background-color: #FF69B4; color: #fff; cursor: pointer; background: linear-gradient(to right, #FF69B4, #FF0000);">
                    Logout
                </button>
            </a>
        </div>
    {% endif %}

    <table class="table table-striped table-hover">
        <thead class="bg-light">
            <tr>
                <th scope="col">Id</th>
                <th scope="col">text</th>
                <th scope="col">Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for contenus in contenu %}
                <tr>
                    <td>{{ contenus.id }}</td>
                    <td>
                        <div class="d-flex align-items-center">
                            <div class="ms-3">    
                                <p class="fw-bold mb-1">{{ contenus.text }}</p>
                            </div>
                        </div>
                    </td>
                    <td>
                        <a href="{{ path('app_moderateur_show', {'id': contenus.id}) }}" class="btn btn-outline-primary">show</a>
                        <a href="{{ path('app_moderateur_edit', {'id': contenus.id}) }}" class="btn btn-outline-success">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="3">no records found</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-4oTk50jNp3u2FVqZl/cLST5DJnI3p7qCUBCMyXaPqz2Lefw83s3dx2I4lRZM8C5a" crossorigin="anonymous"></script>
</body>
</html>
