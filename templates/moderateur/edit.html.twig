{% block title %}Edit Contnu{% endblock %}

{% block body %}
    <h1>Edit Contenu</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_moderateur_home') }}">back to list</a>

    {{ include('moderateur/_delete.html.twig') }}
{% endblock %}