<form method="post" action="{{ path('app_moderateur_delete', {'id': contenu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this contenu?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contenu.id) }}">
    <button class="btn">Delete</button>
</form>